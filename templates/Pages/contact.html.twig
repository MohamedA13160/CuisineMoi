{% extends "base.html.twig" %}

{% block body %}

        <div class="container">
            <h1>Nous contacter </h1>

            <div class="row">

                <div class="col">
                    {{form_start(form)}}
                    {{form_rest(form)}}

                    <button class="btn btn-primary">Envoyer</button>
                    {{form_end(form)}}
                    
                </div>
            </div>

        </div>
            

{% endblock %}